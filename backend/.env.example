DATABASE_URL=postgresql://postgres:postgres@localhost:5432/excalidraw
PORT=3002
# Optional: if set, the app requires this password to be entered once per browser.
# Leave unset/empty for a fully open instance.
APP_PASSWORD=
