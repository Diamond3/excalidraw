services:
  backend:
    image: YOURUSER/excalidraw-backend:latest
    build:
      context: ./self-hosted-backend
    environment:
      DATABASE_URL: ${DATABASE_URL}
    restart: on-failure

  excalidraw:
    image: YOURUSER/excalidraw-frontend:latest
    build:
      context: .
    container_name: excalidraw
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: on-failure
